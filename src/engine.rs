/// This struct holds the chess engine and its functions, so they are not all
/// separate entities in the global space. This is the high level engine "API".
pub struct Engine { }

impl Engine {
    pub fn new() -> Self {
        Self {  }
    }
}
